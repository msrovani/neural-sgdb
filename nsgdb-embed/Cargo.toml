[package]
name = "nsgdb-embed"
version = "1.1.15"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Local embedder for neural-sgdb (ONNX-ready seam, zero-dep hash baseline)"

[dependencies]
neural-sgdb = { path = "..", default-features = false }
