# extension/ — ESTACIONADA (não é o produto)

NSGDB é substrate **agêntico**: MCP 4 tools, doutrina, `agent_protocol`.
Esta pasta é um stub de 1.1.13. Auto-captura (`history`/`tabs`) está
**comentada** em `background.js`. Chrome Web Store **não** é gate de release.

Não zipar, não enviar à Store, não tratar como feature. Reabrir só com ADR.
