# Rust
target/
Cargo.lock

# Connector Python bytecode
__pycache__/
*.py[cod]

# MCP demo db
sgdb_memory.db

# Hot-test MCP memory (Via 1 — agent memory substrate, local-only)
.nsgdb/

# FreeBuff tool state (local-only, not project code)
.freebuff/

# Chrome extension build artifact (gerado via Compress-Archive)
nsgdb-ext.zip
